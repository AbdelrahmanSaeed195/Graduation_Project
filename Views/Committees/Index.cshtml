@model IEnumerable<projectweb.Models.Committee>

@{
    ViewData["Title"] = "إدارة اللجان";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="content-body" dir="rtl">

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show border-0 shadow-sm" role="alert">
            <i class="bi bi-check-circle-fill me-2"></i> @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show border-0 shadow-sm" role="alert">
            <i class="bi bi-exclamation-triangle-fill me-2"></i> @TempData["ErrorMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="h3 mb-0 fw-bold academic-heading"><i class="bi bi-clipboard-check-fill me-2"></i> إدارة اللجان</h1>
        <a asp-action="Create" class="btn btn-academic-primary px-4 shadow-sm">
            <i class="bi bi-plus-circle me-1"></i> إضافة لجنة
        </a>
    </div>

    <div class="card academic-card overflow-hidden shadow-sm border-0" style="border-radius: 12px;">
        <div class="table-responsive">
            <table class="table academic-table table-hover align-middle mb-0 text-center">
                <thead>
                    <tr>
                        <th class="ps-4">رقم اللجنة</th>
                        <th>المكان (القاعة / البلوك)</th>
                        <th class="text-center">السعة الطلابية</th>
                        <th class="text-center">الحالة</th>
                        <th class="text-center pe-4">الإجراءات</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        foreach (var item in Model)
                        {
                            <tr>
                                <td class="ps-4 fw-bold fs-5" style="color: var(--text-main);">@item.CommitteeNumber</td>
                                <td>
                                    @* تعديل عرض المكان ليظهر الأسماء بدلاً من الأرقام فقط *@
                                    <div class="badge bg-light border text-dark px-3 py-2 shadow-sm" style="border-radius: 8px; border-color: var(--border-color) !important;">
                                        <i class="bi bi-geo-alt-fill text-primary me-1"></i>
                                        @if (item.Block != null)
                                        {
                                            <span>قاعة @(item.Block.Hall?.HallName ?? item.Block.HallId.ToString()) / بلوك @item.Block.BlockName</span>
                                        }
                                        else
                                        {
                                            <span class="text-muted">غير محدد</span>
                                        }
                                    </div>
                                </td>
                                <td class="text-center">
                                    <span class="badge px-3 py-2" style="background-color: var(--bg-light); color: var(--text-main); border: 1px solid var(--border-color);">
                                        <i class="bi bi-people-fill me-1"></i> @item.NumberOfStudent طالب
                                    </span>
                                </td>

                                <td class="text-center">
                                    @if (item.StatusOfCommittee == "نشطة")
                                    {
                                        <span class="badge bg-success px-3 py-2" style="border-radius: 6px;">@item.StatusOfCommittee</span>
                                    }
                                    else if (item.StatusOfCommittee == "مغلقة")
                                    {
                                        <span class="badge bg-danger px-3 py-2" style="border-radius: 6px;">@item.StatusOfCommittee</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-warning text-dark px-3 py-2" style="border-radius: 6px;">@item.StatusOfCommittee</span>
                                    }
                                </td>
                                <td class="text-center pe-4">
                                    <div class="d-flex justify-content-center gap-2">
                                        <a asp-action="Details" asp-route-id="@item.CommitteeID" class="btn btn-sm btn-outline-info shadow-sm" title="التفاصيل">
                                            <i class="bi bi-eye"></i>
                                        </a>
                                        <a asp-action="Edit" asp-route-id="@item.CommitteeID" class="btn btn-sm btn-outline-secondary shadow-sm" title="تعديل">
                                            <i class="bi bi-pencil-square"></i>
                                        </a>
                                        <a asp-action="Delete" asp-route-id="@item.CommitteeID" class="btn btn-sm btn-outline-danger shadow-sm" title="حذف">
                                            <i class="bi bi-trash"></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="5" class="text-center py-5 text-muted">
                                <i class="bi bi-folder-x fs-1 d-block mb-2"></i>
                                لا توجد لجان مسجلة حالياً
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>