@model projectweb.ViewModel.StudentCommitteeViewModel

@{
    ViewData["Title"] = "لجنة الطالب";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="row justify-content-center" dir="rtl">
    <div class="col-lg-7">
        <div class="card academic-card">
            <div class="card-header academic-card-header text-white p-4 text-center">
                <h4 class="mb-0 fw-bold text-white"><i class="bi bi-building-check me-2"></i> إشعار توزيع اللجنة</h4>
            </div>
            <div class="card-body p-4" style="background-color: var(--surface-white);">
                <div class="text-center mb-4">
                    <i class="bi bi-person-check-fill text-success" style="font-size: 4rem;"></i>
                    <h5 class="mt-2 fw-bold" style="color: var(--text-main);">@Model.FullName</h5>
                    <p class="text-muted">المستوى الدراسي: @Model.AcademicYear</p>
                </div>

                <div class="row g-3">
                    <div class="col-6">
                        <div class="data-panel text-center">
                            <div class="panel-label">رقم اللجنة</div>
                            <span class="panel-value fs-4 academic-heading">@Model.CommitteeNumber</span>
                        </div>
                    </div>
                    <div class="col-6">
                        <div class="data-panel text-center">
                            <div class="panel-label">رقم الجلوس</div>
                            <span class="panel-value fs-4" style="color: var(--accent-blue);">@Model.SeatNumber</span>
                        </div>
                    </div>
                </div>

                <div class="mt-4 pt-3 border-top text-center" style="border-color: var(--border-color) !important;">
                    <a asp-action="Index" class="btn btn-outline-secondary px-4 fw-bold">العودة لقائمة الطلاب</a>
                </div>
            </div>
        </div>
    </div>
</div>